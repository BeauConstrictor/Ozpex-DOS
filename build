rm imgs/*

for f in programs/*; do
    name=$(basename "$f")
    echo -e "\033[32mAssembling $name...\033[0m"
    vasm -Fbin -dotdir -o "imgs/${name%.*}.img" "$f"
    echo
done

python fs.py
vasm -dotdir -Fbin dos.asm -o rom.bin -esc
